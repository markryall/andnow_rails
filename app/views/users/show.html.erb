<% content_for :navbar do %>
  <ul class="nav">
    <li><%= link_to 'Home', root_url %></li>
    <li><%= link_to 'Sessions', sessions_url %></li>
    <li class="active"><%= link_to 'Profile', user_url %></li>
  </ul>
<% end %>

<div class="row">
  <div class="span1 offset3">
    <img src="<%= gravatar_url session[:email] %>">
  </div>
  <div class="span4">
    <h1><%= session[:email] %></h1>
  </div>
</div>

<%= render 'form' %>